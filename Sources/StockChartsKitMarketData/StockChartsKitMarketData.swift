/// Default market-data providers (Tiingo + Yahoo fallback).
///
/// Real implementation lands in a later issue; this is a placeholder so SPM
/// resolves the target.
public enum StockChartsKitMarketData {}
