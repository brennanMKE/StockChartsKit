# 0007 — Define BrokerageProvider protocol with Capabilities and AuthenticationStatus

| | |
|---|---|
| **Status** | resolved |
| **Module** | Core/Protocols |
| **Platform** | macOS |
| **First seen** | 2026-05-29 |
| **Closed** | 2026-05-29 |
| **Commit** | 736b960 |

## Description

Define the core `BrokerageProvider` actor protocol in `Sources/StockChartsKit/Protocols/BrokerageProvider.swift` (PRD §4) — the single protocol every backend conforms to. Also defines `Capabilities` and `AuthenticationStatus`.

## Scope

- `protocol BrokerageProvider: Actor` with `nonisolated` pure properties `id: ProviderID`, `displayName: String`, `capabilities: Capabilities`, and `nonisolated` injected deps `snapshotStore: (any SnapshotStore)?`, `marketData: (any MarketDataProvider)?`.
- Auth methods: `authenticationStatus()`, `authenticate() -> AuthSession`, `signOut()`.
- Read methods: `listAccounts()`, `positions(for:)`, `balance(for:)`, `quote(for:)`, `priceHistory(symbol:range:)`, `portfolioHistory(accountID:range:)`.
- `Capabilities: OptionSet, Sendable, Codable` with `positions`, `balances`, `quotes`, `nativePriceHistory`, `nativePortfolioHistory`, `crypto`, `realtimeStreaming` (§4).
- `AuthenticationStatus: Sendable` with `connected(connectionID:)`, `requiresChallenge(AuthChallenge)`, `error(BrokerageError)`.
- Depends on protocols #0008 / #0009 for the injected dep types — coordinate ordering (forward refs are fine since they're separate files).

## Acceptance criteria

- Protocol surface matches PRD §4 exactly, keeps `id`/`displayName`/`capabilities` nonisolated so hosts can list providers without actor hops.
- `swift build` passes.

## References

PRD §4 (core protocol), §1 (Swift 6 strict concurrency, actor providers).

## Root cause

New implementation, no prior bug.

## Fix

Added `Sources/StockChartsKit/Protocols/BrokerageProvider.swift`:

- `protocol BrokerageProvider: Actor` with `nonisolated` pure properties `id`,
  `displayName`, `capabilities`, and `nonisolated` injected deps `snapshotStore:
  (any SnapshotStore)?` and `marketData: (any MarketDataProvider)?`; auth methods
  `authenticationStatus()`, `authenticate() -> AuthSession`, `signOut()`; and the
  read methods `listAccounts()`, `positions(for:)`, `balance(for:)`,
  `quote(for:)`, `priceHistory(symbol:range:)`, `portfolioHistory(accountID:range:)`
  with the §4 return types. Signatures match PRD §4 exactly.
- `struct Capabilities: OptionSet, Sendable, Codable` with the seven options at
  bits `1<<0`…`1<<6`.
- `enum AuthenticationStatus: Sendable` with `connected(connectionID:)`,
  `requiresChallenge(AuthChallenge)`, `error(BrokerageError)`.

The default `portfolioHistory` extension is intentionally NOT implemented here;
it is issue 0010.

## Verification

- `swift build` — Build complete, no warnings.
- `swift test` — Test run with 35 tests in 9 suites passed. The "Capabilities
  OptionSet" suite (membership, union/intersection, raw-value bit positions,
  Codable round-trip) and "AuthenticationStatus cases" suite pass.

## Files changed

- `Sources/StockChartsKit/Protocols/BrokerageProvider.swift` (new)
- `Tests/StockChartsKitTests/ContractTests.swift` (new, shared with 0006/0008/0009)
