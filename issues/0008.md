# 0008 — Define MarketDataProvider protocol

| | |
|---|---|
| **Status** | resolved |
| **Module** | Core/Protocols |
| **Platform** | macOS |
| **First seen** | 2026-05-29 |
| **Closed** | 2026-05-29 |
| **Commit** | 736b960 |

## Description

Define `MarketDataProvider` in `Sources/StockChartsKit/Protocols/MarketDataProvider.swift` (PRD §6). The host supplies (or defaults to) a market data source for providers that lack native charting history.

## Scope

- `protocol MarketDataProvider: Sendable` with `quote(symbol:) async throws -> Quote` and `priceHistory(symbol:range:) async throws -> PerformanceSeries`.
- Depends on `Quote`, `Symbol`, `PerformanceSeries`, `TimeRange` (#0003, #0004).

## Acceptance criteria

- Compiles; the concrete `StockChartsKitMarketData` implementation (#0016) and the `FixedMarketDataProvider` test double (#0015) conform to it.
- `swift build` passes.

## References

PRD §6 (market data protocol).

## Root cause

New implementation, no prior bug.

## Fix

Added `Sources/StockChartsKit/Protocols/MarketDataProvider.swift`:
`protocol MarketDataProvider: Sendable` with `quote(symbol:) async throws ->
Quote` and `priceHistory(symbol:range:) async throws -> PerformanceSeries`,
matching PRD §6. Reuses the existing `Quote`, `Symbol`, `PerformanceSeries`,
and `TimeRange` types.

## Verification

- `swift build` — Build complete, no warnings.
- `swift test` — Test run with 35 tests in 9 suites passed. The protocol
  compiles in the core target; concrete/test conformances arrive in later
  issues (0015, 0016).

## Files changed

- `Sources/StockChartsKit/Protocols/MarketDataProvider.swift` (new)
