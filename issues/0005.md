# 0005 — Implement BrokerageError error enum

| | |
|---|---|
| **Status** | resolved |
| **Module** | Core/Errors |
| **Platform** | macOS |
| **First seen** | 2026-05-29 |
| **Closed** | 2026-05-29 |
| **Commit** | f27e5f9 |

## Description

Implement `BrokerageError` (PRD §10) in `Sources/StockChartsKit/Errors/BrokerageError.swift`. Every provider maps its native errors into one of these cases. This is a foundational type that `Money` (#0002) and the protocols depend on, so it should land early.

## Scope

- `enum BrokerageError: Error, Sendable` with cases: `notAuthenticated`, `authenticationFailed(reason:)`, `authorizationRevoked`, `rateLimited(retryAfter: Date?)`, `unsupported(method:)`, `currencyMismatch(lhs: CurrencyCode, rhs: CurrencyCode)`, `providerError(providerID: ProviderID, statusCode: Int?, message:)`, `decodingFailed(underlying: Error)`, `network(underlying: Error)`, `missingSymbol(Symbol)`, `stale(asOf: Date)`.
- Depends on `CurrencyCode`, `ProviderID`, `Symbol` types (#0002, #0004) — coordinate ordering.

## Acceptance criteria

- Compiles; cases match PRD §10 exactly.
- `swift build` passes.

## References

PRD §10 (errors), §18 (mapping native errors).

## Root cause

New implementation, no prior bug.

## Fix

Added `Sources/StockChartsKit/Errors/BrokerageError.swift` with
`enum BrokerageError: Error, Sendable` carrying all §10 cases exactly:
`notAuthenticated`, `authenticationFailed(reason:)`, `authorizationRevoked`,
`rateLimited(retryAfter:)`, `unsupported(method:)`,
`currencyMismatch(lhs:rhs:)`, `providerError(providerID:statusCode:message:)`,
`decodingFailed(underlying:)`, `network(underlying:)`, `missingSymbol(_:)`,
and `stale(asOf:)`. References `CurrencyCode` (Money), `ProviderID`, and
`Symbol` (models), so it lands in the same compile unit as #0002/#0004.

## Verification

Ran `swift build` (Build complete) and `swift test` — 26 tests in 4 suites
passed. `Money`'s `+`/`-` tests in `MoneyTests.swift` exercise
`BrokerageError.currencyMismatch` via `#expect(throws: BrokerageError.self)`,
confirming the case compiles and is thrown.

## Files changed

- `Sources/StockChartsKit/Errors/BrokerageError.swift` — new `BrokerageError`
  enum with all PRD §10 cases.
